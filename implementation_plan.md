# 갓통과 모의고사 서비스 분류 및 관리자 모드 개편 기획안

다양한 형태의 모의고사 서비스(무료 아카이브, 시즌 모의고사, 주간지)를 체계적으로 관리하고, 향후 서비스 확장에 유연하게 대응하기 위한 아키텍처 및 관리자 모드 개편 아이디어입니다.

## 1. 서비스 유형(Service Type) 기반의 데이터 분류

단순히 `exam_id`의 문자열 패턴에 의존하기보다는, 파이어베이스 DB(문서) 레벨에서 명확한 **카테고리(서비스 유형) 필드**를 추가하여 관리하는 것이 좋습니다.

### 📌 분류 체계 제안
각 모의고사 문서(`public_exams`)에 아래와 같은 메타데이터 필드를 추가합니다.

| 서비스 유형 (serviceType) | `exam_id` 네이밍 규칙 (예시) | 인증/결제 필수 여부 | 인앱 문제/해설 제공 | 주요 특징 |
| :--- | :--- | :---: | :---: | :--- |
| **FREE_ARCHIVE** (무료 아카이브) | `past_2028_01` | ❌ (비회원 가능) | ⭕ (앱 내 열람) | 누구나 접근 가능, 트래픽 유입용 |
| **SEASON_MOCK** (시즌 모의고사) | `2028_s0_01` | ⭕ (회원가입/구매) | ❌ (오프라인 배송) | OMR 성적 입력 및 분석 전용 |
| **WEEKLY_MAG** (주간지 시리즈) | `weekly_2028_01` | ⭕ (회원가입/구매) | ❌ (오프라인 배송) | 주기적 발행, 구독형 서비스 연계 |

## 2. 관리자 모드(Admin Mode) UI/UX 구현 아이디어

관리자가 섞여 있는 모의고사들을 쉽게 통제할 수 있도록 UI를 개편합니다.

### ① 모의고사 관리 메뉴 탭(Tab) 또는 필터링 분리
*   현재 하나의 리스트로 쭉 나열되는 '모의고사 관리' 화면 상단에 **[전체보기] [무료 기출] [시즌 모의고사] [주간지]** 탭을 만듭니다.
*   각 탭을 클릭하면 해당 `serviceType`을 가진 모의고사들만 필터링되어 보이게 합니다.

### ② '시험 정보 수정/생성' 모달 창 항목 추가
편집 모달 창에 다음 항목들을 추가하여 세밀한 권한 제어가 가능하도록 합니다.
*   **서비스 카테고리 선택:** (드롭다운) 무료 기출 / 시즌 모의고사 / 주간지
*   **권한 및 접근 제어 토글:**
    *   `로그인 필수 (isLoginRequired)`: 활성화 시 로그인 화면으로 리다이렉트
    *   `유료/구매자 전용 (isPremium)`: 결제 혹은 교재 인증 코드 확인
    *   `문제/해설 PDF 공개 (isContentVisible)`: 앱 내에서 지문을 볼 수 있는지 여부 (시즌 모의고사는 비활성화)

## 3. 사용자(User)용 앱에서의 라우팅 아이디어

관리자 모드에서 설정한 속성들을 바탕으로 사용자 앱의 동작을 자동화합니다.

*   **무료 아카이브 클릭 시:** 곧바로 문제 풀이 창이나 OMR 화면으로 이동.
*   **시즌 모의고사 / 주간지 클릭 시:** 
    *   로그인이 안 되어 있다면 👉 로그인 페이지로 이동
    *   로그인은 했으나 권한(구매 인증)이 없다면 👉 교재 뒤의 '시리얼 코드 인증' 창 띄우기
    *   인증된 사용자라면 👉 "본 교재는 오프라인 전용입니다. OMR 성적을 입력해주세요." 안내 후 OMR 화면으로 이동.

---

## 🙋‍♂️ User Review Required (피드백 요청)

1. **분류 체계 및 네이밍 규칙:** 위에서 제안드린 3가지 카테고리와 `exam_id` 접두어(past, s0, weekly) 규칙이 마음에 드시는지 확인 부탁드립니다.
2. **시리얼 코드/구매 인증 방식:** 유료 서비스인 시즌 모의고사와 주간지의 경우, 앱 내에서 사용자가 '구매자'임을 어떻게 인증하게 할 계획이신가요? (예: 종이 교재에 인쇄된 난수 코드 입력, 온라인 몰 결제 DB 연동 등)
3. **추가 카테고리:** 추후 이 3가지 외에 추가될 수 있는 다른 형태의 서비스(예: 온라인 전용 실전 모의고사)가 더 있을까요?

피드백을 주시면, 이를 바탕으로 관리자 모드(`admin.html`)의 UI 및 DB 저장 로직에 반영하는 작업을 곧바로 진행하겠습니다!
