const puppeteer = require('puppeteer'); const path = require('path'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('BROWSER CONSOLE:', msg.text())); page.on('pageerror', err => console.log('BROWSER ERROR:', err.message)); const filePath = 'file://' + path.resolve('../public/STEST/exam.html'); console.log('Navigating to', filePath); await page.goto(filePath); await page.waitForTimeout(3000); await browser.close(); })();
